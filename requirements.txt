# --- Core CLI & UI ---
# Used for beautiful terminal output and Pro-feature notifications
rich>=13.7.0

# --- YAML & Data Parsing ---
# The heart of our Lexer/Exporter for layout preservation
ruamel.yaml>=0.18.6

# --- Build & Distribution ---

# For creating truly static Linux binaries (optional but recommended)
staticx>=0.14.1; sys_platform == 'linux'

# --- Quality & Development (Optional for users, required for you) ---
# For the bridge to handle dynamic loading and catalog parsing
# pathlib>=1.0.1; python_version < '3.4'