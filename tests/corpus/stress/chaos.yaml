apiVersion: apps/v1
kind: Deployment
metadata:
  name: 🚀-deployment
  annotations:
    desc: "Unicode test: 日本語, 한국어, 👩‍💻"
    garbage: "Some \x00 binary \x01 noise"
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: "quote check \" escaped"
        image: "busybox:latest"
        command: ["sh", "-c", "echo 'Hello 🌍'"]
