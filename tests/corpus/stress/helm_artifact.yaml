apiVersion: apps/v1
kind: Deployment
metadata:
  name: helm-generated

  labels: # Extra newline from bad template
    app: myapp
spec:
  replicas: 1
  template:
    metadata:
    spec:
      containers:
      - name: main
        image: nginx
        env:
          
          - name: EXTRA_SPACE
            value: "true"
          # Helm comment left behind
          - name: DB_HOST
            value: localhost
