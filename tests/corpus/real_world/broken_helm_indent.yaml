apiVersion: apps/v1
kind: Deployment
metadata:
  name: broken-helm-app
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
# BAD INDENTATION HERE (Common copy-paste error) - Should be healed
      - containerPort: 80
        name: http
---
apiVersion: v1
kind: Service
metadata:
  name: broken-helm-svc
spec:
  ports:
  - port: 80
# EXTRA INDENTATION HERE
      targetPort: 80
  selector:
    app: broken-helm-app
